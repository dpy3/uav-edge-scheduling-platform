# 简历项目材料：无人机边缘计算任务调度平台

## 简历项目描述

**无人机边缘计算任务调度平台** | Python、FastAPI、React、PostgreSQL、Docker、OR-Tools

- 面向无人机边缘计算场景建立任务调度模型，统一描述任务计算量、数据量、释放时间、截止时间、优先级，以及节点算力和带宽等异构资源。
- 实现 Greedy、LPT、遗传算法（GA）、OR-Tools CP-SAT 和优先级/截止时间感知启发式算法，在同一任务实例上比较 makespan、求解耗时、任务分配和节点负载。
- 设计 FastAPI 调度接口和 PostgreSQL 运行记录模型，支持 JSON/CSV 场景导入、批量算法对比、历史实验查询和用户数据隔离。
- 使用 React/TanStack Query 构建调度工作台，展示算法结果卡片、逐任务分配、节点负载和历史运行；使用 Docker Compose 编排平台 API、求解服务和数据库。
- 完成多规模、多随机种子、统一时间预算、GA 消融、资源敏感性和动态故障重调度实验，测试覆盖平台后端 68 项、调度服务 14 项。

## 可引用实验结果

在固定随机实例和 5 秒算法时间上限下，平台页面实际运行结果如下：

| 规模 | Greedy | LPT | GA | CP-SAT | 业务启发式 |
| --- | ---: | ---: | ---: | ---: | ---: |
| 20 任务 × 4 节点 | 325 | 394 | 319 | 297（OPTIMAL） | 325 |
| 40 任务 × 5 节点 | 606 | 696 | 666 | 573（FEASIBLE） | 606 |
| 80 任务 × 8 节点 | 572 | 642 | 677 | 747（FEASIBLE） | 568 |

`OPTIMAL` 表示 CP-SAT 已证明最优；`FEASIBLE` 表示在时间预算内找到可行解，不能表述为全局最优。面试时应强调这是精确求解与启发式求解之间的工程权衡，而不是宣称 GA 在所有规模上都优于基线。

## 面试讲解顺序

1. 先说明业务问题：无人机任务需要在本地和多个边缘节点之间分配，同时受到算力、带宽、数据传输和截止时间约束。
2. 再说明模型：任务在节点上的处理时间由计算时间和传输时间组成，优化目标首先是 makespan，并额外记录迟延和负载均衡指标。
3. 然后说明算法：Greedy/LPT 作为快速基线，GA 作为元启发式，CP-SAT 作为中小规模精确基线，业务启发式负责优先级和截止时间场景。
4. 最后说明工程化：平台 API 负责认证、导入、调度和持久化，独立求解服务负责算法执行，前端负责对比和可视化，Docker Compose 完成部署。

## 诚实的结果结论

小规模问题中 CP-SAT 可以快速证明最优，GA 能够接近精确结果；问题规模增大后，CP-SAT 在固定时间预算下只能保证可行解，简单启发式有时反而具有更好的即时 makespan。后续改进方向是更强的 GA 初始化、邻域搜索、截止时间惩罚和能耗/负载多目标优化。
